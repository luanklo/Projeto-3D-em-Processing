# 🧱 Projeto 3D em Processing — Cubo e Pirâmide

## 🎯 Descrição
Este projeto foi desenvolvido em **Processing (modo Java)** com o objetivo de criar uma **cena 3D** composta por dois objetos geométricos:
- Um **cubo**, e  
- Uma **pirâmide**.

A cena utiliza uma **fonte de luz posicional**, aplica o **modelo de iluminação de Phong** e posiciona uma **câmera virtual** para enquadrar os objetos no espaço.

---

## 🖼️ Funcionalidades
- Renderização 3D utilizando o renderer `P3D`.  
- Iluminação baseada no modelo **Phong** (com componentes *ambient*, *diffuse* e *specular*).  
- **Fonte de luz posicional** configurada manualmente com `pointLight()`.  
- **Câmera virtual personalizada** com o comando `camera()`.  
- Rotação contínua dos objetos no espaço 3D.  

---

![]( main-gif.gif )